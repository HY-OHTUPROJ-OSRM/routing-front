import React from "react";

// Currently unused

//import "./HeadLine.css";
//Currently displays Routing pap text, but could be upgraded to use three.js for 3d modelling and making the text spin while zones are updated
const HeadLine = props => {
  return (
    <h2 style={{ position: "absolute", margintop: "15", padding: "0", fontFamily: "sans-serif", overflow: "hidden" }}>Routing app</h2>
  );
};

export default HeadLine; 